spring:
  application:
    name: widget
  mvc:
    static-path-pattern: /static/**
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://dpg-d14hpr95pdvs73f6lso0-a.oregon-postgres.render.com:5432/opt_order_db2}
    username: ${DATASOURCE_USERNAME:opt_order_db2_user}
    password: ${DATASOURCE_PASSWORD:U6ANqCspvLhGcxwBIpWNWCeCCa7w3yPx}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      pool-name: HikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
      leak-detection-threshold: 60000
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
  flyway:
    user: ${FLYWAY_USER}
    password: ${FLYWAY_PASSWORD}
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    baseline-version: 1
logging:
  level:
    org:
      springframework:
        web: DEBUG
        orm: DEBUG
server:
  port: ${PORT:8080}
